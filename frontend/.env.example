# Backend API URL
# Development: http://localhost:8000
# Production: set to your Render web service URL
VITE_API_URL=https://memescope-api.onrender.com

# API key — must match APP_API_KEY set on the backend.
# Leave blank if APP_API_KEY is not configured on the backend.
VITE_APP_API_KEY=
